# Documentação

Use esta pasta para decisões de arquitetura, contratos da API, modelo de dados, diagramas e guias
de deploy.

O contexto detalhado do frontend permanece em `frontend/CLAUDE.md`. Ao iniciar o backend, registrar
aqui os endpoints e os DTOs compartilhados com `frontend/src/services/`.
